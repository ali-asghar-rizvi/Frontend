// -----------------------------------------------------
// -----------------------------------------------------
//-------------------- Answer All ----------------------
// -----------------------------------------------------
// -----------------------------------------------------


// 1. What data type is the following variable ?
const c = "5";

// 2. What data type is the following variable ?
const d = 91;

// 3. Which one is generally better, line 1 or line 2 ?
    let empty1 = undefined; //line 1
    let empty2 = null; //line 2


// 4. What is the console output for the following ?
    let a = "Hello";
    a = "world";
    console.log(a);

// 5. What will be logged to the console ?
    let f = "world";
    let g = `Hello ${f}!`;
    console.log(g);


// 6. What is the value of m ?
    let m = "Hello";
    m = prompt("world");
    console.log(m);

// 7. What is the value of l output to the console ?
    let l = 70;
    l++;
    console.log(l);


// 8. What is the value of result ?
    let result = 3 + 4 * 2 / 8;

// 9. What is the value of total and total2 ?
    let firstNum = 5;
    let secondNum = 10;
    firstNum++;
    secondNum--;
    let total = ++firstNum + secondNum;
    console.log(total);
    let total2 = 500 + 100 / 5 + total--;
    console.log(total2);


// 10. What is logged to the console here ?
    const x = 5;
    const b = 10;
    console.log(x > 0 && b > 0);
    console.log(x == 5 && b == 4);
    console.log(true || false);
    console.log(x == 3 || b == 10);
    console.log(x == 3 || b == 7);









// -------------------Miles to kilometers converter-------------------
// ----------------------------------------------
// Create a variable that contains a value in miles, convert it to kilometers, and
// log the value in kilometers in the following format:






// -------------------BMI calculator-------------------
// ----------------------------------------------
// Set values for height in inches and weight in pounds, then convert the
// values to centimeters and kilos, outputting the results to the console:
// 1 inch is equal to 2.54 cm
// 2.2046 pounds is equal to 1 kilo
// Output the results.Then, calculate and log the BMI: this is equal to weight
// (in kilos) divided by squared height(in meters).Output the results to theconsole.